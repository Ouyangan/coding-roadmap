apiVersion: v1
kind: De
metadata:
  name:
  labels:
    app:
spec:
  replicas: 1
  template:
    metadata:
      name:
      labels:
        app:
    spec:
      containers:
        - name:
          image:
          imagePullPolicy: Allways
      restartPolicy: Always
  selector:
    matchLabels:
      app: 
